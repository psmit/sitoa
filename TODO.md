 - Instead of recalculating everything from scratch every find-moves and find-solution distance, make struct that stores info like clusters for incremental updates
 - Make negamax function iterative instead of recursive
 
